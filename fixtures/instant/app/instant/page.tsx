// The literal boolean form of the instant contract - the one shape crust can
// read with no inference at all.
export const instant = true

export default function Page() {
  return <main>declares an instant navigation contract</main>
}
