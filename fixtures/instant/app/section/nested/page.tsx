// Declares nothing itself. Its recorded config must still carry the layout's
// `prefetch`, keyed by the layout file.
export default function Page() {
  return <main>inherits its prefetch from the layout above</main>
}
